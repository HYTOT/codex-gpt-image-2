# GPT Image 2 16格 LINE 贴图排版任务｜line_sticker_sheet_v5

请基于输入参考图，生成一张高质量的 16 格 LINE 贴图总排版大图。输出必须是一张已经完成排版的正方形贴图成片，不是 16 张分开的单图，不是海报，不是宣传图，不是舞台截图。

## 人物主体

{{subject_block}}

## 风格要求

{{style_block}}

## 排版结构

{{layout_block}}

## 16 格贴图内容

{{sticker_cells_block}}

## 语义约束

{{semantic_exclusion_block}}

## 背景与贴纸效果

{{background_block}}

## 文字质量

{{text_quality_block}}

## 可选补充要求

{{extra_requirements}}

## 超强结构锁定

- 参考图 1 只提供真人头部、五官、发型、发色、耳环和整体气质
- 参考图 2 是全套 16 格唯一的身体模板来源，只提供厨师身体、厨师帽、举牌构图、牌子相对位置、手臂角度、牌柄握法与镜头裁切逻辑
- 参考图 3 不再提供任何身体、手臂、帽子或裁切参考，只保留第 2 格红色 `✗` 的语义和颜色语言参考
- 最终 16 格都必须是同一种固定主体：真人头接到 reference_002 的厨师身体与厨师帽上，而不是自由 Q 版公仔身体
- 全 16 格必须保持 reference_002 那一类厨师身体模板、帽子高度、牌子位置、手臂角度、牌柄长度、裁切比例和牌面边框系统
- 16 格区别主要只允许落在牌面内容、牌面颜色和脸部细微神态上，不允许每格大改 pose、视角、朝向或身体比例
- 持牌手必须尽量是正常 5 根手指，不允许沿用参考图 2 和参考图 3 的简化玩具手
- 头部与厨师帽必须自然衔接，头颈和领口必须自然连接，不允许漂浮头、空脖子、断层、穿模或压脸
- 真人辨识度优先级高于风格化夸张，高于装饰丰富度，高于表情差异幅度
- 最终成片只能包含 16 个贴图单元，必须严格等于 16 个，不能是 15 个，也不能是 17 个或更多
- 版式必须是 4 行 × 4 列的清晰均匀网格，按从左到右、从上到下依次排布
- 整张图中唯一允许出现的牌面内容，就是任务里指定的 16 条符号或文字；每条只能出现 1 次，不可改字，不可扩写，不可补充额外口号

## 多参考图使用规则

- 参考图 1 是人物身份主参考，只用于锁定真人头部与五官
- 参考图 2 是厨师身体模板唯一来源，只用于锁定全套身体、帽子、手臂、举牌姿态和稳定相对位置
- 参考图 3 只用于给第 2 格提供红色 `✗` 的语义和颜色语言，不再提供任何身体体态
- 参考图 2 和参考图 3 的手部简化风格不能直接照搬；最终人物持牌手必须按正常 5 指绘制
- 第 1 格和第 2 格可以分别借鉴绿色 `✓` 与红色 `✗` 的牌面颜色语言，但第 2 格体态仍必须与 reference_002 主模板一致

## 人物与模板硬锁定

- 最终主体不是“Q 版人物自由拿牌”，而是“真人头自然接到厨师身体和厨师帽上的固定模板”
- 头部必须明显保留参考图 1 的偏窄鹅蛋脸、自然大眼、真实鼻口比例、黑色半扎长发、银色圆耳环和温柔轻笑气质
- 发型需要适配厨师帽，但不得丢掉黑发与耳环识别点
- 不允许把厨师帽画成漂浮帽，不允许切掉额头，不允许把头塞进帽子里
- 不允许把厨师身体改成别的衣服类型，不允许漂移回上一套白衣灰袖公仔身体，也不允许把体态做成通用公仔举牌 pose
- 全图尽量固定为单手举牌，另一只手自然垂落或贴身；若表情变化与手部稳定冲突，优先保单手举牌和五指手
- 第 1 格只允许绿色 `✓`，第 2 格只允许红色 `✗`，都不允许任何中文
- 第 3 格到第 16 格的 2 到 4 字牌面优先单行显示；必要时只允许轻微缩字，不允许做成怪异竖排、海报标题或乱码
- 牌子设计必须建立在 reference_002 的圆牌加手柄体系上，只允许微调边框、材质、字重、字号与配色的精致度，不允许中度重设计、更换牌型、放大牌或异形牌

## 硬性限制

- 整张图必须是完整的 4×4 均匀网格，总共 16 格，不多不少
- 每格都必须是同一个固定主体的变体，而不是不同角色或不同身体模板
- 头帽衔接和领口衔接必须自然
- 持牌手必须尽量正常五指
- 第 2 格虽然是红色 `✗`，但身体、手臂、帽子和持牌姿态也必须与 reference_002 主模板一致
- 第 1 格和第 2 格整格都不允许任何中文文字，只允许 `✓` 与 `✗`
- 第 3 格到第 16 格必须分别显示指定牌面内容
- 不要复刻参考图原始背景，不要生成海报、舞台截图、摄影拼图、聊天气泡或额外口号

补充负向限制：

{{negative_prompt}}
